[project]
name = "nft-api"
version = "0.1.0"
requires-python = ">=3.13"
dependencies = [
    "nft-shared",
    "fastapi>=0.115",
    "uvicorn[standard]>=0.32",
    "redis[hiredis]>=5.0",
]

[tool.uv.sources]
nft-shared = { path = "../shared", editable = true }